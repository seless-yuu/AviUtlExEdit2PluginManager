# ユーティリティスクリプト利用ガイド

AviUtlExEdit2PluginManager v0.2 のビルド・開発支援スクリプト集

## 概要

本プロジェクトには、開発効率を向上させるための6つのバッチファイルが含まれています。
これらのスクリプトを使用することで、プロジェクトのセットアップから本番デプロイまでを自動化できます。

## スクリプト一覧

### 1. setup.bat - プロジェクト初期化
**用途**: 新規プロジェクトの環境構築
**実行タイミング**: プロジェクトの最初に1回

**機能**:
- 必要なディレクトリ構造の作成
- `.gitignore` ファイルの生成
- サンプルソースコード (`main.cpp`) の作成
- ビルド設定ファイル (`CMakeLists.txt`, `Makefile`) の作成
- 設定ファイル (`config/settings.ini`) の作成

### 2. dev-build.bat - 開発ビルド
**用途**: デバッグ情報付きのビルド実行
**実行タイミング**: 開発中の動作確認時

**機能**:
- デバッグモードでのコンパイル
- `bin/debug` フォルダへの出力
- ビルド後の実行オプション提示
- 複数ビルドシステム対応 (Visual Studio, .NET, Make)

### 3. build.bat - 本番ビルド
**用途**: 最適化された配布用ビルド
**実行タイミング**: リリース準備時

**機能**:
- 最適化ビルドの実行
- `bin/release` および `dist` フォルダへの出力
- 配布用ファイルの自動コピー
- バージョン情報ファイルの生成
- 配布用ZIPパッケージの作成オプション

### 4. run.bat - プログラム実行
**用途**: ビルド済みプログラムの実行
**実行タイミング**: テスト・動作確認時

**機能**:
- 実行バージョンの選択 (Debug/Release/Distribution)
- 自動バージョン検出
- 実行結果の確認
- ログファイルの表示オプション

### 5. test.bat - テスト実行
**用途**: プロジェクトのテスト実行
**実行タイミング**: 開発中の品質確認時

**機能**:
- C++テストファイルのコンパイル・実行
- 環境チェックテスト
- テスト結果のレポート生成
- 複数テスト形式対応

### 6. clean.bat - クリーンアップ
**用途**: 不要ファイルの削除
**実行タイミング**: ディスク容量確保時、新規ビルド前

**機能**:
- ビルド成果物の削除
- 一時ファイルの削除
- IDE設定ファイルの削除
- 選択的クリーンアップ

## 推奨ワークフロー

### 初回セットアップ
```
1. setup.bat を実行
2. src/main.cpp を編集してコードを実装
3. dev-build.bat でビルドテスト
```

### 日常開発
```
1. コード編集
2. dev-build.bat でビルド
3. run.bat で動作確認
4. test.bat でテスト実行
```

### リリース準備
```
1. test.bat で全テスト実行
2. build.bat で本番ビルド
3. 配布パッケージの確認
```

### トラブルシューティング
```
1. clean.bat でクリーンアップ
2. setup.bat で環境再構築（必要に応じて）
3. 再ビルド
```

## 対応環境

- **OS**: Windows 10/11
- **ビルドツール**: 
  - Visual Studio 2019/2022
  - .NET 5.0以降
  - MinGW/MSYS2
  - CMake 3.16以降
- **言語**: C++17対応コンパイラ

## 注意事項

- スクリプトは管理者権限での実行が必要な場合があります
- 初回実行時は必要な依存関係を手動でインストールしてください
- ネットワーク環境によってはパッケージダウンロードに時間がかかる場合があります

## カスタマイズ

各スクリプトはプロジェクトの要件に応じてカスタマイズ可能です。
特に以下の項目は環境に合わせて調整してください:

- コンパイラオプション
- 出力ディレクトリ
- 依存ライブラリの設定
- 配布パッケージに含めるファイル

## サポート

スクリプトに関する問題や改善提案は、GitHubのIssueでお知らせください。